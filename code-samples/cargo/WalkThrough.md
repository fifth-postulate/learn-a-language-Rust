# Cargo Walk Through

Start with

```sh
cargo help
```

Go on with

```sh
cargo help new
```

Create a binary

```sh
cargo new --vcs git --name walkthrough --bin walkthrough
```

Enter directory `cd walkthrough`

Show build options

